#include<stdio.h>
int main()
{
    char a[50], b[50];
    printf("Enter a string");
    scanf("%s", a);
    printf("Enter a string");
    scanf("%s", b);
    printf("the output is %s, %s", a, b);
    return 0;
}
